import React, { ReactNode } from "react";
import "./App.css";

export const App = (): ReactNode => {
  return (
    <div className="center font-1">
      Welcome to our own CRA using webpack & babel
    </div>
  );
};
